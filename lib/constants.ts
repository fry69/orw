/**
 * Global version string
 */
export const VERSION = "0.6.0";

/**
 * OpenRouter API URL
 */
export const OPENROUTER_API_URL = "https://openrouter.ai/api/v1/models";

/**
 * Main watcher check interval
 */
export const WATCHER_INTERVAL_MS = 3_600_000; // one hour in milliseconds

/**
 * Fetch timeout
 */
export const FETCH_TIMEOUT_MS = 4_000; // 4 seconds in milliseconds

/**
 * UI refresh interval
 */
export const UI_REFRESH_MS = 60_000; // one minute in milliseconds
