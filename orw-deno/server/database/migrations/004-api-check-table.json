{
  "description": "Create last_api_check table for tracking API status",
  "statements": [
    {
      "description": "Create table to store last API check timestamp",
      "sql": "CREATE TABLE IF NOT EXISTS last_api_check (id INTEGER PRIMARY KEY, last_check TEXT NOT NULL)"
    }
  ],
  "version": 4
}
